#!/usr/bin/python3
# 5-number_keys.py


def number_keys(a_dictionary):
    """
    Return the count of keys in a dictionary.

    Args:
        a_dictionary (dict): The dictionary to count keys from.

    Returns:
        int: The number of keys in the dictionary.
    """
    return len(a_dictionary)
